const ResizeButton = ({ onClick }) => (
  <button className="button-style modal-button" onClick={onClick}>
    <i className="fa-solid fa-up-down-left-right"></i>
  </button>
);

export default ResizeButton;
